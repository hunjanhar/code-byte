apiVersion: v1
kind: Service
metadata:
  name: code-byte-service
  namespace: code-byte-ns
  labels:
    app: code-byte
spec:
  selector:
    app: code-byte
  ports:
    - name: http
      port: 80
      targetPort: 80

    - name: metrics
      port: 9117
      targetPort: 9117
